<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.intranet.ssmcrmserver.dao.mapper.MenuV2Mapper">
    <resultMap id="BaseResultMap" type="com.intranet.ssmcrmserver.dao.model.MenuV2" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="idx" property="idx" jdbcType="INTEGER" />
        <result column="superior_id" property="superiorId" jdbcType="INTEGER" />
        <result column="pattern" property="pattern" jdbcType="INTEGER" />
        <result column="menu_name" property="menuName" jdbcType="VARCHAR" />
        <result column="abbr" property="abbr" jdbcType="VARCHAR" />
        <result column="router_path" property="routerPath" jdbcType="VARCHAR" />
        <result column="icon" property="icon" jdbcType="VARCHAR" />
        <result column="open_style" property="openStyle" jdbcType="INTEGER" />
        <result column="is_init" property="isInit" jdbcType="INTEGER" />
        <result column="is_del" property="isDel" jdbcType="INTEGER" />
    </resultMap>

    <sql id="base_column_list">
        id, idx,superior_id,pattern,menu_name,abbr,router_path,icon,open_style,is_init,is_del
    </sql>

    <sql id="where_clause">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="superiorId != null">
            and superior_id =#{superiorId}
        </if>
        <if test="pattern != null">
            and pattern =#{pattern}
        </if>
        <if test="openStyle != null">
            and open_style=#{openStyle}
        </if>
        <if test="routerPath != null">
            and router_path=#{routerPath}
        </if>
        <if test="isInit != null">
            and is_init = #{isInit}
        </if>
        <if test="isDel != null">
            and is_del = #{isDel}
        </if>
    </sql>

    <select id="selectOneByPrimaryKey" resultMap="BaseResultMap">
        select
        <include refid="base_column_list" />
        from menuV2
        where id = #{id}
    </select>

    <select id="selectOneBySelective" resultMap="BaseResultMap">
        select
        <include refid="base_column_list" />
        from menuV2
        where 1=1
        <include refid="where_clause"></include>
    </select>

    <select id="selectListBySelective" resultMap="BaseResultMap">
        select
        <include refid="base_column_list" />
        from menuV2
        where 1=1
        <include refid="where_clause"></include>
        order by idx
    </select>

    <update id="update" parameterType="com.intranet.ssmcrmserver.dao.model.OperationAuthority">
        update menuV2
        <set>
            <if test="isDel != null">
                is_del=#{isDel}
            </if>
        </set>
        where 1=1
        <if test="id != null">
            and id = #{id}
        </if>
    </update>
</mapper>